# secdata-pipeline requirements

# LLM API client
openai>=1.30.0

# Configuration
PyYAML>=6.0

# Progress bars
tqdm>=4.65.0

# Embeddings for deduplication
sentence-transformers>=2.5.0
numpy>=1.24.0
torch>=2.0.0

# Optional: vLLM if you're serving the model on the same machine
# vllm>=0.5.0
